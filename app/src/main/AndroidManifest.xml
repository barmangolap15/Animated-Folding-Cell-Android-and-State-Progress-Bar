<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codewithbrahim.example">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".qbuttons.QButtonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExtraActivity" />
        <activity android:name=".StateProgressActivity">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity android:name=".UsagePricingActivity" />
        <activity android:name=".UsageAmenitiesActivity" />
        <activity android:name=".UsageCompleteActivity" />
        <activity android:name=".UsageStatusActivity" />
        <activity android:name=".AnimatedBottomNavBarActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>